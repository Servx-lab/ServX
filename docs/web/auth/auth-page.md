# Auth page (sign-in / sign-up)

**Route:** `/auth`  
**File:** `apps/web/src/features/auth/AuthPage.tsx` (re-exported from `features/auth/index.tsx`)

Hosts Firebase-powered authentication UI (email/password, Google, etc., depending on Firebase config). On success, navigation continues to dashboard or onboarding according to app logic.
