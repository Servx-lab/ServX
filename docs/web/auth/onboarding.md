# Onboarding

**Route:** `/onboarding`  
**File:** `apps/web/src/features/auth/Onboarding.tsx`

First-run or guided setup experience **without** the main dashboard sidebar (separate layout branch in `App.tsx`). May compose **`OnboardingModal`** and related UI from the same feature folder.
