# Package: `@servx/types` (`packages/types`)

Shared **TypeScript types** for connections, users, GitHub repo summaries, database enums, etc.

**Consumed by:** web (via path alias `@servx/types` in Vite) and API.

**Entry:** `packages/types/index.ts`

See the file for full definitions (`UserConnection`, `User`, `RepoSummary`, `DatabaseType`, …).
